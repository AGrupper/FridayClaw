# 2026-04-20

## Session: end-session + start-session generalization

**What was done:**
- Generalized `end-session` skill: replaced hardcoded Jarvis paths/Python script with auto project-name detection, 3-step repo cascade (git remote → GitHub AGrupper/* search → conversation fallback), Notion MCP for debrief. Works for any project without edits.
- Generalized `start-session` skill: same treatment — dropped `session_start.py`, adopted subagent + Notion MCP pattern with same detection rules.
- Tightened project-name rule: true version suffixes (`Jarvis1.0` → `Jarvis`) stripped, domain suffixes (`sleep.io`) preserved.
- Saved paste-ready Mac versions: `mac-end-session.md` and `mac-start-session.md` in `~/.claude/plans/` for copy to Mac.

**Pending (user action):**
- Paste Mac skill files into `~/.claude/commands/` on MacBook.
