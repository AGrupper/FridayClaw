# 2026-04-24

- Verified the Friday production cron path end to end after the local-model fallback work.
- Locked the working architecture to `qwen3:1.7b` as the OpenClaw cron wrapper and `falcon3:3b` as the internal Friday router.
- Renamed the primary local-routing CLI flag to `--use-local-router` and kept `--use-granite` as a deprecated compatibility alias.
- Updated Friday docs and implementation notes so they describe the real split between the outer wrapper and the inner router.
- Trimmed the active OpenClaw model catalog to the production path and removed local `granite3.3:2b` after post-removal verification passed.
- Confirmed repeated cron runs stayed `ok` on the Ollama `qwen3:1.7b` wrapper with no GPT fallback in recent run history.
